<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
    android:background="@color/color_text_white">

    <ExpandableListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toLeftOf="@+id/rlv_filter_type"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rlv_filter_type"
        android:layout_width="120dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:background="@color/color_bbbbbb"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:background="@color/color_text_white"
        android:orientation="horizontal"
        android:paddingLeft="8dp"
        android:paddingTop="10dp"
        android:paddingRight="8dp"
        android:paddingBottom="10dp"
        android:elevation="10dp">

        <Button
            android:id="@+id/btn_filter_reset"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginRight="10dp"
            android:layout_weight="1"
            android:background="@color/color_button_gray"
            android:text="@string/reset"
            android:textColor="@color/color_text_white"
            android:textSize="14sp"
            android:elevation="10dp"/>

        <Button
            android:id="@+id/btn_filter_complete"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginLeft="10dp"
            android:layout_weight="1"
            android:background="@color/color_primary"
            android:text="@string/complete"
            android:textColor="@color/color_text_white"
            android:textSize="14sp"
            android:elevation="10dp"/>
    </LinearLayout>

</RelativeLayout>